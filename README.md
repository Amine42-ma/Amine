# DINO ROYALE EVOLUTION

**لعبة بقاء أونلاين بمنظور الشخص الأول، بأسلوب Block Style عالي الجودة، مع ديناصورات أصلية وعالم مفتوح ضخم.**

An original, fully online, first-person block-style survival battle royale. Everything —
world, creatures, weapons, textures, sounds, animations — is generated procedurally at
runtime from original designs. **No assets, maps, models, sounds, names or files are taken
from any other game.**

---

## ما الذي يميّز هذا المشروع

| | |
|---|---|
| **عميل واحد** | كل اللعبة داخل `index.html` واحد (لا مكتبات خارجية، لا CDN، لا ملفات صور أو صوت) |
| **يعمل بلا خادم** | المتصفحات تتصل ببعضها مباشرة عبر WebRTC — لا تثبيت، لا حساب، لا بطاقة |
| **أو بخادم مرجعي** | Node.js بلا أي تبعيات لمن يريد ضماناً كاملاً ضد الغش |
| **أونلاين بالكامل** | لا يوجد وضع أوفلاين؛ كل مباراة تُحاكى بسلطة واحدة بمزامنة 30 Hz |
| **عالم ضخم** | 6144 × 6144 بلوك، 16 منطقة حيوية، بدون شاشات تحميل بين المناطق |
| **بدون بث للتضاريس** | كل الأطراف تولّد نفس العالم من نفس البذرة، فلا يُنقل سوى تعديلات اللاعبين |

---

## وضعان للّعب

اللعبة تختار الوضع تلقائياً عند الفتح، ولا تسألك شيئاً.

### 1. مباشر بين المتصفحات (الافتراضي) — بلا خادم إطلاقاً

افتح `index.html` من أي مكان: ملف على جهازك، أو Netlify، أو GitHub Pages، أو أي
استضافة ملفات ثابتة. اضغط **«مباراة عشوائية»** فتحصل على رمز غرفة من ٦ خانات،
أرسله لأصدقائك أو شاركهم الرابط، ويدخلون معك مباشرة.

- بلا تسجيل دخول · بلا بطاقة · بلا تنصيب · يعمل على الهاتف
- من يفتح الغرفة يصبح **المضيف**: متصفحه يشغّل نفس محاكاة المباراة التي يشغّلها
  الخادم بالضبط (`server/lib/match.js` نفسه)، والبقية يرسلون له نواياهم فقط.

**المقابل بصراحة:** المضيف هو الحَكَم، فبإمكانه نظرياً التلاعب. بقية اللاعبين
يخضعون للتحقق الكامل — لا طيران، ولا إطلاق عبر الجدران، ولا غنائم من العدم.
لغرفة بين أصدقاء هذه مقايضة مقبولة؛ للمنافسة الجادة استخدم الوضع الثاني.

> يحتاج هذا الوضع وسيطاً صغيراً ليتعارف المتصفحان (SDP فقط، ولا يمر به أي شيء من
> اللعب). تستخدم اللعبة وسطاء MQTT عموميين مجانيين وتنتقل بينهم تلقائياً إن سقط
> أحدهم. لا حساب ولا مفاتيح.

### 2. خادم مرجعي — ضمان كامل ضد الغش

إن كانت الصفحة مخدومة من خادم اللعبة نفسه، تستخدمه تلقائياً بدل الاتصال المباشر.

| النظام | الطريقة |
|---|---|
| ويندوز | انقر مرتين على `start.bat` |
| ماك / لينكس | انقر مرتين على `start.sh` (أو `./start.sh` من الطرفية) |

```bash
node --version     # يجب أن يكون 20 أو أحدث - لا توجد تبعيات أخرى
npm start          # أو:  node server/server.js
# ثم افتح http://localhost:8080
```

إذا لم يكن Node.js مثبتاً، حمّله من [nodejs.org](https://nodejs.org).

لإجبار وضع معيّن: `localStorage['dre.netMode'] = '"p2p"'` أو `'"server"'`.

### اللعب من الهاتف

الهاتف **لا يستطيع تشغيل الخادم بنفسه**، لذلك تنزيل `index.html` وفتحه من
التنزيلات لن يعمل أبداً. أمامك طريقان:

**أ. عبر شبكة الواي فاي المنزلية** (الأسرع للتجربة)

1. شغّل الخادم على حاسوب متصل بنفس الواي فاي: `npm start`
2. سيطبع الحاسوب عنواناً مثل `http://192.168.1.20:8080`
3. افتح ذلك العنوان في متصفح الهاتف

**ب. رابط عالمي بنقرة واحدة — بلا حساب وبلا بطاقة** ★

```
online.bat     (ويندوز)
./online.sh    (ماك / لينكس)
```

يشغّل الخادم ثم يفتح أمامه نفق Cloudflare Quick Tunnel ويطبع رابطاً عاماً
`https://…trycloudflare.com` يعمل من أي هاتف في أي مكان. أداة النفق تُنزَّل
تلقائياً أول مرة (~37 ميجابايت) وتُخزَّن في `.cloudflared/`.

لماذا هذا الخيار موجود أصلاً: اللعبة تحتاج عملية Node حيّة تحتفظ باتصالات
WebSocket، والاستضافات الثابتة المجانية لا تستطيع ذلك إطلاقاً، بينما أغلب
الطبقات المجانية التي تستطيع صارت تطلب بطاقة. النفق لا يحتاج حساباً ولا
بطاقة، وجهازك على الأرجح أقوى من أي نسخة مجانية.

المقابل بصراحة: الرابط يعيش ما دامت النافذة مفتوحة، ويتغيّر مع كل تشغيل.
للحصول على رابط ثابت استخدم الاستضافة أدناه.

**ج. استضافة دائمة** (رابط ثابت لا يتغيّر)

المشروع جاهز للنشر بلا أي تعديل:

| المنصة | الطريقة |
|---|---|
| **Render** | ارفع المستودع على GitHub ← New ← Blueprint ← اختر المستودع. ملف `render.yaml` جاهز. |
| **Railway / Fly.io / Koyeb** | يكتشفون `Dockerfile` تلقائياً |
| **VPS** | `docker build -t dino . && docker run -p 80:8080 -v dino-data:/data dino` |

بعدها تحصل على رابط مثل `https://dino-royale.onrender.com` — افتحه من أي هاتف أو
حاسوب ويعمل كل شيء: «العب الآن»، المطابقة، المحادثة الصوتية.

> على الاستضافة المجانية في Render يكون القرص مؤقتاً، أي أن الحسابات تُمسح عند
> إعادة النشر. اربط قرصاً دائماً واجعل `DATA_DIR` يشير إليه للاحتفاظ بها.

#### ✅ Netlify / GitHub Pages / Vercel تعمل الآن

هذه الخدمات تستضيف **ملفات ثابتة** فقط ولا تشغّل أي عملية خادم — ولهذا كانت
اللعبة تفشل عليها سابقاً. مع الوضع المباشر بين المتصفحات لم يعد ذلك مهماً: ارفع
`index.html` وحده وسيعمل كل شيء، لأن المحاكاة تجري داخل متصفح المضيف والاتصال
مباشر بين اللاعبين.

الشيء الوحيد الذي تخسره هناك هو **الخادم المرجعي** (الحماية الكاملة من غش
المضيف). إن أردته، انشر على منصة تشغّل Node.js — **Render** أو **Railway** أو
**Fly.io** أو أي VPS، كما في الجدول أعلاه.

للعب مع أصدقاء على نفس الشبكة، افتح العنوان الذي يطبعه الخادم (`http://<ip>:8080`).
للنشر على الإنترنت ضع الخادم خلف عاكس HTTPS (nginx / Caddy) وفعّل ترقية WebSocket.

### متغيرات البيئة

| المتغير | الافتراضي | الوصف |
|---|---|---|
| `PORT` | `8080` | منفذ HTTP + WebSocket |
| `HOST` | `0.0.0.0` | عنوان الاستماع |
| `DATA_DIR` | `./data` | مجلد تخزين الحسابات والعشائر |
| `MAX_CONNECTIONS_PER_IP` | `12` | حد الاتصالات لكل عنوان |
| `PUBLIC_URL` | — | العنوان العام (مطلوب لتسجيل الدخول الاجتماعي) |
| `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` | — | تفعيل الدخول بجوجل |
| `FACEBOOK_APP_ID` / `FACEBOOK_APP_SECRET` | — | تفعيل الدخول بفيسبوك |
| `SESSION_SECRET` | عشوائي | توقيع معاملات OAuth |

---

## تسجيل الدخول

### 1. العب الآن — بدون أي تسجيل ⚡

الزر الأول في الشاشة. نقرة واحدة → داخل اللوبي خلال أقل من نصف ثانية. لا بريد،
لا كلمة مرور، لا إعداد من طرفك. **هذا هو المسار الافتراضي لمعظم اللاعبين.**

الحساب حقيقي تماماً: مستوى، خبرة، إنجازات، عملات، أصدقاء، إحصائيات — كلها تُحفظ
على الخادم. وعند إعادة فتح اللعبة يدخل اللاعب مباشرة بلا أي شاشة.

القيد الوحيد: الحساب مرتبط بمتصفح هذا الجهاز. لذلك يظهر تنبيه لطيف في اللوبي
يعرض **«تأمين الحساب»** — يضيف اسماً وبريداً وكلمة مرور **دون فقدان أي تقدّم**،
فيصبح الحساب دائماً ويعمل على أي جهاز.

### 2. الاسم/البريد + كلمة المرور

تعمل دائماً بدون أي إعداد. الاسم أو البريد يعملان في نفس الحقل.

### 3. Google / Facebook (اختياري)

زرّان **لا يظهران إطلاقاً** حتى تضع بيانات الاعتماد أنت — مرة واحدة، وليس على
كل لاعب. اللاعب يضغط الزر فقط. اللعبة كاملة بدونهما.

### تفعيل الدخول بجوجل

1. افتح [Google Cloud Console → Credentials](https://console.cloud.google.com/apis/credentials)
2. أنشئ **OAuth client ID** من نوع **Web application**
3. أضف في *Authorized redirect URIs* بالضبط:
   `https://عنوانك/auth/google/callback`
4. ضع القيم في البيئة:
   ```bash
   export PUBLIC_URL=https://عنوانك
   export GOOGLE_CLIENT_ID=...
   export GOOGLE_CLIENT_SECRET=...
   ```

### تفعيل الدخول بفيسبوك

1. افتح [Facebook for Developers → My Apps](https://developers.facebook.com/apps)
2. أنشئ تطبيقاً وأضف منتج **Facebook Login**
3. في *Valid OAuth Redirect URIs* ضع:
   `https://عنوانك/auth/facebook/callback`
4. ضع `FACEBOOK_APP_ID` و `FACEBOOK_APP_SECRET` في البيئة

> فيسبوك وجوجل يرفضان عناوين `http://` عدا `localhost`. للتجربة محلياً استخدم
> `PUBLIC_URL=http://localhost:8080`.

### كيف تُربط الحسابات

- أول دخول بمزوّد → يُنشأ حساب جديد بدون كلمة مرور، ويُشتق الاسم من ملفك.
- إذا كان بريد المزوّد **مطابقاً لحساب موجود** → يُربط بنفس الحساب (لا تكرار).
  بريد جوجل يُقبل فقط إذا كان موثّقاً (`email_verified`).
- من **الإعدادات ← الحساب** يمكنك إضافة كلمة مرور، أو ربط/فك ربط مزوّد.
  فك الربط مرفوض إذا كان سيتركك بلا وسيلة دخول.

**الأمان:** معامل `state` موقّع بـ HMAC وله عمر 10 دقائق (حماية CSRF)، ورمز
الجلسة لا يمر أبداً في الرابط — يُستبدل تذكرة أحادية الاستخدام عمرها 60 ثانية
عبر WebSocket، ثم تُمسح من شريط العنوان.

---

## التحكّم

### لوحة المفاتيح والفأرة

| المفتاح | الوظيفة | المفتاح | الوظيفة |
|---|---|---|---|
| `W A S D` | الحركة | الفأرة | النظر |
| `Shift` | ركض | زر أيسر | إطلاق |
| `Space` | قفز / سباحة | زر أيمن | تصويب |
| `Ctrl` | انحناء | `R` | إعادة تعبئة |
| `C` | انزلاق | `F` | تفاعل / التقاط |
| `Z` | انبطاح | `V` | ضربة قريبة (ضغط مستمر = صوت) |
| `Q` / `E` | ميل يسار / يمين | `B` | وضع البناء |
| `1 2 3` | تبديل السلاح | `G` | عجلة الإشارات |
| `4` / `5` / `H` | علاج / درع / قنبلة | `X` | عجلة الحركات |
| `Tab` | لوحة النتائج | `Enter` | محادثة |
| `F3` | إحصائيات الأداء | `Esc` | قائمة الإيقاف |
| `F4` | إظهار/إخفاء أزرار اللمس | | |

### اللمس (الهاتف والأجهزة اللوحية)

تظهر الأزرار تلقائياً عند بدء المباراة على أي جهاز لمس
(`pointer: coarse` + `maxTouchPoints > 0`)، ويمكن إجبارها أو تعطيلها من
**الإعدادات ← التحكّم**.

- **عصا تناظرية** أسفل اليسار: منطقة ميتة 16٪، مدى 82٪، والسرعة تتناسب مع مقدار
  الدفع — دفعة نصفية تمشي، ودفعة كاملة تركض. القيمة تُرسل للخادم كبايتين
  (`int8`) ضمن حزمة الإدخال، فالخادم يبقى صاحب القرار في السرعة.
- **منطقة سحب** تغطي الشاشة للنظر، مع حساسية قابلة للضبط.
- **عشرة أزرار** دائرية: إطلاق، تصويب، قفز، انحناء، ركض، تعبئة، تفاعل، بناء،
  علاج، ضربة قريبة — أزرار الحالة (تصويب/انحناء/ركض/بناء) مثبّتة بضغطة.
- **ثلاث خانات أسلحة** تعكس السلاح المُختار مباشرة، وصف علوي للنتائج والإشارة
  والحركات والمحادثة والقائمة.
- **وضع الأعسر** يقلب التخطيط بالكامل، و**حجم الأزرار** قابل للتكبير.
- **مساعدة تصويب** خفيفة (مخروط 7°/4.5° مع فحص خط رؤية): تسحب النظر بلطف نحو
  الهدف، ولا تلتقط الهدف تلقائياً ولا تطلق النار أبداً — والخادم يتحقق من كل
  إصابة كالمعتاد، فلا تمنح أي أفضلية غير مشروعة.

> أزرار اللمس موضوعة خارج `#hud` في شجرة DOM عمداً: الواجهة مكدّسة بعناصر
> مطلقة الموضع، ووجود الأزرار داخلها كان يعني أن إبهاماً قد يصيب شريط صحة بدل
> زر الإطلاق.

---

## بنية المشروع

```
index.html               ← اللعبة كاملة: الواجهة، المحرك، الشبكة، الصوت (ملف واحد)
server/
  server.js              ← HTTP + WebSocket + توجيه الرسائل
  lib/
    ws.js                ← تنفيذ WebSocket كامل (RFC 6455) بلا تبعيات
    store.js             ← تخزين مستندي مع كتابة ذرّية
    accounts.js          ← الحسابات، الجلسات، الاسترجاع، الخبرة، الإنجازات
    social.js            ← الأصدقاء، المجموعات، العشائر، النقابات، المحادثة
    matchmaking.js       ← الطوابير، الغرف الخاصة، حلقة المحاكاة
    match.js             ← محاكاة المباراة المرجعية (فيزياء، قتال، ديناصورات، العاصفة)
    voice.js             ← توجيه المحادثة الصوتية وإشارات WebRTC
    worldgen.js    (مشترك) ← توليد العالم الحتمي
    protocol.js    (مشترك) ← صيغة الشبكة وثوابت اللعب
    content.js     (مشترك) ← الأسلحة، الأدوات، الديناصورات، التقدّم
    movement.js    (مشترك) ← فيزياء الشخصية (تنبؤ + مرجعية)
    oauth.js             ← تسجيل الدخول بجوجل/فيسبوك (OAuth 2.0)
start.bat / start.sh     ← تشغيل بنقرة واحدة (ويندوز / ماك ولينكس)
Dockerfile / render.yaml ← نشر جاهز على أي استضافة
tools/build-client.js    ← يُدرج الوحدات المشتركة داخل index.html
test/run.js              ← اختبارات الوحدة
test/integration.js      ← عميلان حقيقيان عبر البروتوكول الفعلي
```

الملفات المعلّمة بـ **(مشترك)** مُدرجة حرفياً داخل `index.html`. بعد أي تعديل عليها:

```bash
node tools/build-client.js      # يعيد إدراجها
node tools/build-client.js --check   # يتحقق فقط (للـ CI)
npm test                        # يتأكد أن النسختين متطابقتان على 120 ألف عيّنة
```

---

## المحرك الرسومي (WebGL2)

المسار الكامل لكل إطار:

1. **Depth + normal prepass** — early-Z ويغذّي الإظلام المحيطي
2. **SSAO** بنصف الدقة ثم تنعيم منفصل المحورين
3. **ظلال متتالية** (3 شرائح، 2048²، مثبّتة على البكسل لمنع الاهتزاز)
4. **سماء إجرائية** — تشتّت Rayleigh/Mie، قرص الشمس، القمر، النجوم، طبقتا سحاب
5. **تضاريس PBR** — GGX + Smith، خرائط normal، إضاءة voxel محسوبة بـ BFS، ضباب ارتفاعي
6. **نباتات alpha-tested** مع حركة رياح
7. **مياه** — انعكاسات فضاء الشاشة، انكسار، امتصاص حسب العمق، رغوة الشاطئ، بريق الشمس
8. **جسيمات** ناعمة (soft particles) بمزج إضافي
9. **معالجة لاحقة** — Bloom، TAA بإعادة إسقاط زمني، ACES tonemap، تدرّج لوني، FXAA

كل الخامات (61 مادة × albedo + normal + roughness/metal/emissive) تُولَّد رياضياً عند
الإقلاع — لا يوجد ملف صورة واحد في المشروع.

**متطلبات:** متصفح يدعم WebGL2 (Chrome/Edge 90+، Firefox 90+، Safari 15+).

---

## الشبكة ومنع الغش

الخادم هو المصدر الوحيد للحقيقة. العميل يرسل **النوايا فقط** (أزرار + زوايا النظر)،
ولا يرسل أبداً موقعه أو إصاباته أو غنائمه.

- **30 Hz** محاكاة على الخادم، **15 Hz** لقطات للعميل
- **تنبؤ محلي** لشخصية اللاعب باستخدام نفس ملف الفيزياء الذي ينفّذه الخادم، ثم مصالحة
  بإعادة تشغيل المدخلات غير المؤكَّدة (خطأ صفري عند التنبؤ الصحيح)
- **استيفاء زمني** 110 ms لبقية اللاعبين والديناصورات
- **تعويض التأخر** بإرجاع الخصوم حتى 260 ms عند حساب الإصابات
- **تحقق من كل شيء**: معدل الإطلاق، الذخيرة، مدى البناء، مدى التفاعل، حدود العالم
- **إدارة الاهتمام**: لا تُرسل إلا الكيانات ضمن 220 م
- **إعادة اتصال تلقائية** بتراجع أُسّي؛ الجسد يبقى في العالم 120 ثانية

لأن التضاريس حتمية على الجانبين، فإن حركة الشبكة داخل المباراة تقتصر على اللاعبين
والكيانات وتعديلات البلوكات — وليس على العالم نفسه.

---

## المحادثة الصوتية

الصوت ينتقل **مباشرة بين اللاعبين** عبر WebRTC (Opus، مع إلغاء الصدى وكبح الضوضاء)،
بينما يبقى الخادم مسؤولاً عن **من يسمع من**:

- `proximity` (Solo) — صوت مكاني ثلاثي الأبعاد ضمن 42 م
- `team` (Duo/Squad) — الفريق فقط
- `all` (Apex Hunt) — الجميع

يُطلب إذن الميكروفون **مرة واحدة عند أول تشغيل**؛ القرار يُحفظ في الحساب. الرفض يُبقي
الميكروفون مغلقاً تماماً مع إمكانية سماع الآخرين. يوجد كتم ذاتي، كتم لاعب معيّن (يُحفظ)،
وضع اضغط-للتحدث أو ميكروفون مفتوح.

الخادم يرفض تمرير إشارات WebRTC بين لاعبَين لا يشتركان في قناة — فلا يمكن التنصّت على
فريق الخصم.

---

## المحتوى

- **16 منطقة حيوية**: محيطات، شواطئ، سهول، غابات، أدغال، تايغا، ثلوج، صحارى، سافانا،
  مستنقعات، جبال، براكين، أنهار، جزر، أراضٍ وعرة، أنهار جليدية
- **7 أنواع معالم** مولَّدة إجرائياً: معابد، قواعد مهجورة، قرى مدمرة، مناجم عميقة،
  خزائن سرية، أعشاش، منصات بحرية — لكل واحدة تخطيط مختلف مشتق من بذرتها
- **8 ديناصورات أصلية** بسلوك مختلف (قطعان، كمائن، طيران، سباحة، زعيم)
- **13 سلاحاً + 23 أداة** مع 6 مستويات ندرة
- **61 نوع بلوك** قابل للبناء والتدمير
- **5 أنماط لعب**: Solo / Duo / Squad / Apex Hunt / غرفة خاصة

---

## الاختبارات

```bash
npm test
```

تغطي: مصافحة WebSocket مقابل متجه RFC، تطابق توليد العالم بين العميل والخادم على
120 ألف voxel، دقة تكميم البروتوكول، حتمية الفيزياء، دورة حياة الحساب والاسترجاع
والتقدّم، تسجيل الدخول الاجتماعي وربط الحسابات، ومحاكاة مباراة حية (انضمام،
مدخلات، لقطات، قتال، عاصفة، نتائج) — **71 اختبار وحدة + 40 فحص تكامل**.

---

## الأداء

| الإعداد | الوصف |
|---|---|
| `low` | بدون ظلال/توهج/TAA، مقياس 0.72 |
| `medium` | ظلال منخفضة، SSAO، توهج |
| `high` (افتراضي) | ظلال 3 شرائح، SSAO، توهج، TAA، انعكاسات مياه |
| `ultra` | نفس `high` مع جسيمات أكثر |

التقطيع يعمل داخل Web Workers (عدد الأنوية − 1) مع تحميل حلزوني من موقع اللاعب للخارج،
فلا توجد شاشة تحميل بين المناطق.

---

## الترخيص

MIT — انظر `LICENSE`. جميع التصاميم والأسماء والقيم أصلية لهذا المشروع.
